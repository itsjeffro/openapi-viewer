interface Routes {
  [key: string]: {
    name: string
  }
}

const routes: Routes = {
  'csrf-token': {
    name: 'Csrf Token'
  },
  'legal-entity': {
    name: 'Legal Entity'
  }
}

export default routes
